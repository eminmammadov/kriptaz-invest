// Export ProjectSlider component for easy importing
export { default as ProjectSlider } from './ProjectSlider';

// Re-export types
export type {
  ProjectSliderProps,
  ProjectLogo,
  ProjectSliderState
} from '@/shared/types/projectSlider';
